Written a HTML/JavaScript program, which takes the
URL of an XML document containing the top online Newspaper
information, parses the XML file, and extracts the list of top 
online newspapers, displaying them in a table. The JavaScript 
program is embedded in an HTML file so that it can be executed 
within a browser.